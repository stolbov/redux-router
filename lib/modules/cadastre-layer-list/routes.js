import React from 'react';
import { Route } from 'react-router';
import CadastreLayerList from './components/CadastreLayerList';

export default (
  <Route path="/cadastre-layer-list" component={ CadastreLayerList } />
);
